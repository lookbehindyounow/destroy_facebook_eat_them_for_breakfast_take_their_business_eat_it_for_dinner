{% extends 'base.jinja' %}{% block content %}
<div class="focused_container">
    {# user's name #}
    <p>{{post.content}}</p>
    <hr/>
    {# likes CALL THEM APPROVALS HAHA #}
    {% for comment in comments %}
        <p>{{comment.content}}</p>
        <br/>
    {% endfor %}
    <a href="/{{post.id}}/new_comment">comment</a>
</div>
{%endblock%}