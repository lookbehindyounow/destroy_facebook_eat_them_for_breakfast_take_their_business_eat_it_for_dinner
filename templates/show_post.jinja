{% extends 'base.jinja' %}{% block content %}
    {# user's name #}
    <h3>{{post.content}}</h3>
    <hr/>
    {# likes CALL THEM APPROVALS HAHA #}
    {% for comment in comments %}
        <p>{{comment.content}}</p>
        <br/>
    {% endfor %}
{%endblock%}